<Window x:Class="DatabaseBackupManager.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:local="clr-namespace:DatabaseBackupManager"
xmlns:vm="clr-namespace:DatabaseBackupManager.ViewModels"
xmlns:views="clr-namespace:DatabaseBackupManager.Views"
mc:Ignorable="d"
Title="SQL Server Backup &amp; Restore Tool" 
Height="800" 
Width="1000"
MinHeight="600"
MinWidth="800"
FlowDirection="RightToLeft"
WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- DataTemplates for ViewModel to View mapping -->
        <DataTemplate DataType="{x:Type vm:ConnectionViewModel}">
            <views:ConnectionView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:BackupViewModel}">
            <views:BackupView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RestoreViewModel}">
            <views:RestoreView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScheduleViewModel}">
            <views:ScheduleView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FieldManagerViewModel}">
            <views:FieldManagerView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DataTransferViewModel}">
            <views:DataTransferView />
        </DataTemplate>

        <!-- Navigation Button Style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#FF3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF1ABC9C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFBDC3C7"/>
                                <Setter Property="Foreground" Value="#FF7F8C8D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Navigation -->
        <Border Grid.Row="0" Background="#FF2C3E50" Padding="10,10,10,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Application Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🗄️" FontSize="24" Margin="0,0,10,0"/>
                    <TextBlock Text="پشتیبانی و بازیابی اطلاعات" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White" 
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Navigation Buttons -->
                <Border Grid.Column="1" Padding="10,0,10,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="🔗 ارتباط با سرور" 
                                Command="{Binding NavigateToConnectionCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                        <Button Content="💾 پشتیبانی" 
                                Command="{Binding NavigateToBackupCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                        <Button Content="📥 بازیابی" 
                                Command="{Binding NavigateToRestoreCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                        <Button Content="📅 زمانبندی جهت پشتیبانی گیری" 
                                Command="{Binding NavigateToScheduleCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                        <Button Content="📋 افزودن فیلد جدید" 
                                Command="{Binding NavigateToFieldManagerCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                        <Button Content="📤 انتقال داده" 
                                Command="{Binding NavigateToDataTransferCommand}"
                                Style="{StaticResource NavButtonStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Connection Status Indicator -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#FFE74C3C"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ConnectionViewModel.IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="#FF2ECC71"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Foreground="White" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="قطع ارتباط"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ConnectionViewModel.IsConnected}" Value="True">
                                        <Setter Property="Text" Value="متصل"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Row="1" Background="#FFF5F6FA" Padding="20">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#FF34495E" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Message -->
                <TextBlock Grid.Column="0" 
                           Text="{Binding CurrentViewModel.StatusMessage}" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>

                <!-- Error Message -->
                <TextBlock Grid.Column="1" 
                           Text="{Binding CurrentViewModel.ErrorMessage}" 
                           Foreground="#FFE74C3C" 
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="20,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentViewModel.HasError}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
